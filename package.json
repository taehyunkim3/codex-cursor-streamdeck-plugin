{
  "name": "codex-stream-deck",
  "version": "0.1.0",
  "private": true,
  "description": "Stream Deck plugin that shows local Codex session activity on keys.",
  "scripts": {
    "preview": "node com.local.codex-status.sdPlugin/bin/plugin.js --preview",
    "check": "node --check com.local.codex-status.sdPlugin/bin/plugin.js"
  }
}
